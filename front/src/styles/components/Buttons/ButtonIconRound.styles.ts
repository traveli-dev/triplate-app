import { theme } from "@/styles/theme";
import { css } from "@emotion/react";

export const styles = {
    button:css`
    width:40px;
    height:40px;
    background: ${theme.color.white};
    border:0;
    border-radius:100px;
    `
}