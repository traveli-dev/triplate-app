import { css } from '@emotion/react'
import { theme } from '@/styles/theme'

export const styles = {
  layoutLinkItem: css`
    margin-top: 8px;
  `,
  text: css`
    font-size: ${theme.fontSize.md};
  `
}
